import SwiftUI

enum FFTheme {
    enum Radius {
        static let card: CGFloat = 22
        static let control: CGFloat = 14
    }

    enum Shadow {
        static let color = Color.black.opacity(0.09)
        static let radius: CGFloat = 20
        static let y: CGFloat = 8
    }
}
